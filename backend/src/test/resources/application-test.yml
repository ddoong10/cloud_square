spring:
  datasource:
    url: jdbc:h2:mem:lms;MODE=MySQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
    open-in-view: false

app:
  jwt:
    secret: test-jwt-secret-for-local-build-only
  cors:
    allowed-origin: https://lms.uoscholar-server.store
  static-base-url: https://static.uoscholar-server.store
  demo:
    user:
      email: demo@lms.local
      password: demo1234!
